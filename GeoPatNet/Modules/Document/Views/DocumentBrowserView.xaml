<UserControl x:Class="Emash.GeoPatNet.Modules.Document.Views.DocumentBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinWidth="600"
             MinHeight="400"
             d:DesignHeight="300" d:DesignWidth="300">
    <TabControl ItemsSource="{Binding Path=Codes,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Path=Libelle,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"></TextBlock>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>        
            <DataTemplate>
                <Grid Background="LightGray" Drop="ContentControl_Drop" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AllowDrop="True">

                </Grid>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>
