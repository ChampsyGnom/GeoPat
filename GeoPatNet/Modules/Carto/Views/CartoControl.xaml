<UserControl x:Class="Emash.GeoPatNet.Modules.Carto.Views.CartoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:dsctrl="clr-namespace:DotSpatial.Controls;assembly=DotSpatial.Controls"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <Button Click="btModeZoom_Click" x:Name="btModeZoom">
                <TextBlock Text="Mode Zoom"></TextBlock>
            </Button>
            <Button Click="btModeMove_Click"  x:Name="btModeMove">
                <TextBlock Text="Mode déplacement"></TextBlock>
            </Button>
        </ToolBar>
        <WindowsFormsHost FocusManager.IsFocusScope="True" Focusable="True"  Grid.Row="1" IsHitTestVisible="False"   HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Beige" x:Name="mapHost">
            <dsctrl:Map  BackColor="AliceBlue" x:Name="map">
                
            </dsctrl:Map>
        </WindowsFormsHost>
    </Grid>
</UserControl>
